# BE A PRO, הודעת פתיחה לצ'אט חדש

אני איציק. אנחנו ממשיכים עבודה על המשחק שלי BE A PRO, משחק מנג'ר כדורגל ישראלי בעברית (RTL),
Vite + React + TypeScript, ב-`C:\Users\itzik\Desktop\CLAUDE\be-a-pro`. תשתמש בסקיל `be-a-pro-skill`.
הצ'אט הקודם נגמר. ההודעה הזו מתארת בדיוק איפה עצרנו. **תקרא הכל לפני שאתה נוגע במשהו.**

**המשחק באוויר ואנשים אמיתיים משחקים בו.** כל שינוי שאתה דוחף מגיע לאנשים תוך שלוש דקות.

---

## מצב הגיט

הריפו: `github.com/beapromanager/beapromanager.github.io`, ענף `main`, ה-remote נקרא **game**
(`git push game main`). ה-origin הישן הוא ארכיון, לא דוחפים אליו.
האתר החי: https://beapromanager.github.io/ (base `/`). GitHub Actions בונה מ-main, עדכון תוך 2-3 דקות.
לעקוב: `gh run list --repo beapromanager/beapromanager.github.io --limit 1`.
`gh` מחובר בתור itzik200592-byte. הגדרות Pages עושה איציק בדפדפן.

**אין קומיטים מקומיים שלא נדחפו. main ו-game/main שניהם על `35ef937`.**

**הצעד הראשון שלך:** `git status` (חייב להראות רק `M .claude/launch.json`, סשן אחר הוסיף שם config
של green-bio, לא נוגעים ולא מכניסים לקומיט), `git log --oneline game/main..main` (חייב להיות ריק),
ואז להריץ את הסוויטה (למטה). אם git status מראה משהו אחר, עצור ושאל. אחרי שהכל ירוק תגיד "מוכן"
ותשאל מה הבא.

---

## הדפוס שלנו (חשוב)

אני נותן משימה. לפני שאתה מתקן, **תחקור לעומק, תגיד מה מצאת ומה הסדר המומלץ, ותחכה שאאשר.**
תיקון אחד בכל פעם, קומיט לכל תיקון.

**תמדוד, אל תניח.** אם אני טוען "X קורה", תריץ סימולציה ותגיד את המספר. ואם המדידה שלך מפתיעה,
תבדוק שהיא לא רועשת מדי לפני שאתה מדווח אותה.

**אל תנחש למה משהו שבור. תשחזר אותו קודם.** בשני הסשנים האחרונים כל באג אמיתי נמצא ככה, וכל פעם
שניחשתי טעיתי. בסשן הזה זה קרה שוב: "קריסה בייצור" התבררה כקריסה על שרת פיתוח, והוכחתי את זה בניסוי.

**כשגומרים תיקון:** הסוויטה הרלוונטית ירוקה, `npx tsc --noEmit`, `npm run build` (ואז
`git checkout -q tsconfig.tsbuildinfo`), אימות בדפדפן למה שנראה במסך, commit מקומי בהודעה בסגנון שלנו
(פרוזה באנגלית שמסבירה **למה**, לא רשימת שינויים, מסתיימת ב-
`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`), ואז אתה שואל **"GIT PUSH?"**.
דוחפים רק אחרי שאני כותב GIT PUSH (גם "FIT PUSH" זה GIT PUSH). `git add` עם שמות קבצים מפורשים, לא `-A`.

**טקסטים במשחק: כתיבה ביחד.** אתה מציע, אני מתקן, אתה מיישם **בדיוק** במילים שלי. אל תתקן לי שגיאות
כתיב על דעת עצמך, תסמן אותן ותשאל. טקסט שהכנסת בלי אישור מראש: טבלת "היה / עכשיו" בסיכום.
**אין מקפים ארוכים בשום טקסט שהשחקן רואה.**

**איזון וכלכלה:** אתה מציג מספרים ואפשרויות, אני מחליט.

**בסוף כל תיקון:** מה תוקן, מה הבדיקה מודדת, איך אימתת שהיא נכשלת על קוד שבור, מה ראית בדפדפן,
ודברים שצפו ולא נגעת בהם.

---

## הכלל על בדיקות (חשוב מאוד)

כל בדיקה חדשה חייבת להיות **מאומתת שהיא נכשלת על קוד שבור**: משבשים בכוונה, רואים FAIL עם ההודעה
הנכונה, משחזרים, ומאמתים `cmp` מול עותק טוב. הדרך שעובדת: לפני הסבוטאז' `cp` של הקובץ לעותק `.good`
בסקראצ'פאד; סבוטאז' עם סקריפט שמחליף מחרוזת ומוודא שהעוגן מופיע בדיוק פעם אחת; שחזור עם `cp` מהעותק
הטוב (לא `git checkout <file>`); `cmp` בסוף. **בדיקה שעוברת בפעם הראשונה חשודה.**

**בדיקה שלא יכולה להיכשל גרועה מאין בדיקה.** קרה בסשן הזה: טענה על שליחה ברשת עברה תמיד, כי הבקשה
הראשונה עוד הייתה באוויר והשנייה נחסמה על ידי מנעול פנימי ולא על ידי ההגנה שנבדקה. הוספתי המתנה של
טיק אחד וווידאתי שהיא באמת נופלת.

**שומרים שנכשלו על עצמם:** שומר שמחפש מחרוזת בקוד תופס לפעמים את ההערה של עצמו. תשתמש בהתאמת מחרוזת
פשוטה (`includes`) ולא ברגקס כשאפשר, כי ההימלטויות נבלעות כשכותבים רגקס לתוך קובץ דרך תבנית מחרוזת.
זה קרה לי שוב בסשן הזה, ב-lineup-check.

---

## הסוויטה, 48 בדיקות

```
for s in pitch formations sync goals numbers debt economy climb sacking guaranteed league youth kits palette names market lineup press invite gems coach install scroll summer legends seasonkit penalty discipline back voice shape oldsave repeat suspension keeper part dilemma sub tutorial pack feed seed ads sponsor report fans friends telemetry; do npm run -s check:$s >/dev/null 2>&1 && echo "OK   $s" || echo "FAIL $s"; done
```

לוקחת 20-35 דקות (discipline לבד 7-17 דק'). תריץ ב-run_in_background עם הפלט לקובץ ב-
`C:/Users/itzik/AppData/Local/Temp/` ושורת DONE בסוף. **בזמן שהסוויטה רצה אל תערוך קבצי מקור**
(הבדיקות מייבאות אותם בזמן ריצה). בדיקה בודדת: `npm run -s check:<name>`.

**save ישן:** כל שדה חדש על ה-state חייב ברירת מחדל ב-`src/game/save.ts` (loadCareer) וגם ב-
`ADDED_SINCE` ב-`scripts/oldsave-check.mts`.

**מחיקת מסך = סכנה לשחקן חי.** אם מוחקים phase, שמירה שעומדת עליו נפתחת על כלום. יש דפוס קיים ב-
`save.ts` (`patched.phase === 'match'` ו-`=== 'vs'`) שממפה אותה למסך שאחריו. תמיד להוסיף מיפוי כזה.

---

## מה קרה בסשן הזה (11 קומיטים, `5f98951` → `35ef937`)

### א. הטקסטים של החברים (4693fe6)
שלושה טקסטים שאיציק אישר: השורה בשוק אומרת עכשיו "זה מהכרטיס שלו **במסך הסגל**", שורת האישור היא
"מועדון אחר יסתכל רק על הדירוג שלו, לא על מי שהוא", והכרטיס של השבוע אחרי מתאים את עצמו לדרך הפרידה
(מכרת מול שיחררת), כי הוא קרא "מכרת" גם על שחרור בהסכמה. שומר דו כיווני ב-check:friends.

### ב. החבר שעוזב חותם ביריבה (2fc9162)
מי מהשניים שעוזב מנותב ל**יריבת הדרבי** (רק הוא, מכירה רגילה נשארת כפי שהייתה), וכרטיס נוחת במסך
הבית: "כהן חתם ביריבה / אורי כהן חתם בטירת כרמל. בדרבי הקרוב הוא יעמוד מולך, והיציע שלהם כבר יודע
מאיפה הוא הגיע." **נמדד:** כל 92 הערים בכל 5 הדרגים (460 ליגות) תמיד יש יריבת דרבי, ו-`rivalId` של
המועדון שלך הוא תמיד אותו מועדון. check:friends עלה ל-100 בדיקות.

### ג. הקרדיט במסך הפתיחה (59348a9)
במקום "גרסת פיתוח": **"המשחק נעשה ע״י ITZIK UZIEL, ISRU · כל השמות והמועדונים בדיוניים"**.
השם באנגלית עטוף בבידוד כיווניות. משפט השמות הבדיוניים נשאר בכוונה, הוא ההגנה המשפטית.

### ד. הדלת: 40% פחות משקל בכניסה ראשונה (5cc5014)
הנפילה הכי גדולה במשחק היא מי שפותח ולא לוחץ "התחל קריירה". **נמדד:** הכניסה הורידה 989 קילובייט,
מתוכם **721 ארבע תמונות הפתיח**, והן התחילו לרדת רק ב-380ms, אחרי שכל החבילה ירדה והורצה, וארבעתן
יחד. עכשיו: הפריים הראשון מוכרז ב-HTML (`<link rel="preload">`) ומתחיל ב-**66ms** יחד עם הקוד,
השלושה האחרים נטענים רק אחרי שהוא נחת, והתמונות קודדו מחדש ברוחב האמיתי (719KB → 328KB, ללא הבדל
נראה לעין). ואם הפריים הראשון לא הגיע תוך `FIRST_FRAME_BUDGET` (1200ms) הפתיח נזרק והולכים ישר למסך
הפתיחה. **אומת** על ידי הפניית הפריים הראשון לכתובת שלא ניתנת לניתוב.

### ה. המסדרון למשחק: שש לחיצות לשלוש (f140960)
הדרך ממסך הבית עד כדור ראשון הייתה 6 לחיצות ב-5 מסכים, **ושניים מהם אמרו את אותו משפט סקאוטינג מילה
במילה**. נמחק `src/ui/screens/Vs.tsx` (כרטיס הסמלים), והדילמה של העיתונות ממשיכה מעצמה אחרי 2.2 שניות
במקום לדרוש לחיצה שנייה (התגובה של הכתב נשארת על המסך להיקרא, היא הטעם בבחירה).
**נמדד:** המחזור הראשון עובר דרך שאלת עיתונות ב-30 מתוך 30 קריירות.

### ו. שרת הפיתוח הפסיק להיספר כשחקן (5dd0a97)
היה באג אמיתי: הרצה מקומית דיווחה למסד הייצור, אז כל קריירה שהלכנו לבדיקה נספרה כאדם, וכל שגיאה
במחשב נספרה כקריסה של שחקן, **וכפול**, כי React מריץ אפקט פעמיים בפיתוח. עכשיו
`servedLocally()` ב-`src/game/telemetry.ts` מחזיר true על localhost, לולאה, `.local` וטווחים פרטיים,
ואז `endpoint()` מחזיר מחרוזת ריקה, שזה מצב שהקוד כבר ידע להתנהג בו. **המשמעות בשבילך: אתה יכול
לשחק במשחק מקומית כמה שאתה רוצה בלי ללכלך נתונים.** check:telemetry עלה ל-63 בדיקות.

### ז. מסך הפתיחה, טקסט ועיצוב (3ce6ed9, 540b011, c3c3d1c)
הטקסט של איציק (הכרטיס בוואטסאפ ומסך הפתיחה אמרו **את אותן שתי שורות מילה במילה**, אז המסך לא נתן
שום סיבה חדשה ללחוץ):
> **מהשכונה ועד אירופה**
> קח את הקבוצה של העיר שלך מליגה ג׳. תקבע הרכב, תקנה או תמכור שחקנים ותענה לעיתונות בחוכמה.
> חוויה ישראלית אמיתית:
> היציע מדבר אלייך, השכונה מגיבה למעשים שלך, והעיתונות מחכה שתיפול.

והעיצוב נבנה מחדש אחרי שאיציק אמר שזה לא יפה: הלוגו ממוסך אל תוך האור (היה ריבוע עם קצה נראה), הטקסט
בשתי רמות עם קו זהב דק, הזהב רק על כפתור אחד (שורת ההתקנה הפכה ניטרלית), שלוש הקופסאות הפכו לרצועה
אחת מחולקת, ונוספו זרקורים, ויניאטה וגרעין. כניסה מדורגת דרך `.title-rise` עם `--i`, ו-
`prefers-reduced-motion` מכבה אותה לגמרי (**אומת בכרום עם המצב כפוי**, אחרת המסך היה יכול להיות ריק).

### ח. חדר ההלבשה נהיה עריך (5f0bb93, 35ef937)
לוח הגיר היה תצוגה בלבד. עכשיו לוחצים על מי שיוצא ואז על מי שנכנס, קורא לשני הכיוונים, ושניים מההרכב
מחליפים עמדות. **כל החלפה עוברת דרך `G.swapBlockedReason`**, אותו כלל שמסך הסגל משתמש בו, וסירוב נאמר
באדום מתחת ללוח. הטקסט של איציק:
> **חדר ההלבשה**
> רגע לפני שעולים למגרש זה הזמן לבחור את ההרכב.
> לחץ על שחקן בהרכב ואז על מי שייכנס במקומו.

---

## הנתונים החיים, נכון לסיום הסשן

**203 מכשירים, 448 פתיחות, 22 חזרו ביום אחר, ואפס קריסות של שחקנים** (שתי שורות הקריסה בטבלה הן
מהפיתוח של ה-24). 118 התחילו קריירה, 56 שיחקו מחזור, **29 התחילו עונה שנייה**.

**החתך שמדד את התיקונים** (הפריסות: הדלת 25.9 ב-09:12, המסדרון ב-09:44, הדליפה ב-09:52 שעון ישראל):

| | לפני | אחרי |
|---|---|---|
| פתחו ולחצו "התחל קריירה" | 53% | 51% |
| הגיעו למסך הבית ושיחקו מחזור | 74% | **87%** |
| זמן ממסך הבית עד סיום מחזור ראשון | 8.8 דק' | **6.3 דק'** |

**המסקנה החשובה:** הדלת לא זזה למרות שהיא נטענת 40% יותר מהר, ומתוך 95 שרק פתחו, **83 לא פתחו שוב
אף פעם**. מי שנשבר מעמוד איטי מנסה שוב. הם החליטו, לא חיכו. לכן ההימור הבא שם הוא **מילים ולא הנדסה**,
וזה בדיוק מה שנעשה בערב האחרון (הטקסט והעיצוב), ועוד לא נמדד.

**איך קוראים את הנתונים:** דרך ה-MCP של Cloudflare, `d1_database_query`, מסד `beapro-count`,
מזהה `695c34b0-42ad-4fda-b7c4-ab5b39a98922`. **אל תנסה לפתוח את הדשבורד בדפדפן, ראה למטה.**

---

## אזהרה שתחסוך לך שעה: workers.dev חסום אצל איציק

הדשבורד (`https://beapromanager.github.io/?admin=beapro-rahat-hetsi-maamad-derbi-8988`) מציג לו את
המשחק במקום את הנתונים. **זה לא באג בקוד.** מדדתי: ה-DNS של הרשת שלו (הוא מחובר דרך שיתוף מהטלפון)
**בולע כל שם שמסתיים ב-workers.dev**, כולל הדומיין העירום, בזמן ש-github.io ו-example.com נפתרים
רגיל, ו-8.8.8.8 ו-1.1.1.1 פותרים את הכל. השרת חי, הקוד תקין, המפתח נכון, ואימתתי את הדשבורד עצמו
עובד על ידי עקיפה (`--host-resolver-rules`).

**המשמעות הרחבה:** שחקנים שהרשת שלהם חוסמת באותה צורה משחקים מצוין אבל לא נספרים. המספרים הם רצפה.
**הפתרון שממתין להחלטה:** להעביר את ה-Worker ל-`pages.dev`, שנבדק ולא חסום אצלו, עם אותו מסד ואותם
נתונים (Pages Functions תומך ב-D1). ראה "מה פתוח".

---

## מה פתוח, בסדר שהמלצתי

1. **הכרטיס בוואטסאפ עדיין נושא את ההבטחה הישנה.** `og:description` ב-`index.html` אומר "קח קבוצה
   מליגה ג׳ ותוביל אותה עד אלופת אירופה. טירוף ישראלי, יציע שלא סולח, וחלום אחד גדול." זה הטקסט
   הראשון שזר קורא, לפני שהוא בכלל מגיע למשחק. צריך טקסט מאיציק.
2. **רעיון האבוקות לפני דרבי.** איציק רוצה תמונה של אבוקות ביציע לפני דרבי או משחק עלייה, ואמר שנעשה
   את התמונה ביחד. **ההמלצה הטכנית שכבר על השולחן:** שתהיה פעימה של שתי שניות שעוברת מעצמה, כמו
   הפתיח, ולא מסך עם כפתור, כדי לא להחזיר לחיצה למסדרון שקיצרנו. צריך ממנו: אילו משחקים בדיוק, ואיך
   התמונה נראית.
3. **הדשבורד החדש.** איציק אישר בעין מוקאפ של תצוגה משופרת: הסבר מתחת לכל מספר, משפט שמפרק את היחס
   בין מכשירים לפתיחות, קופסה שמראה כמה מהמספרים הם המכשיר שלו, והודעת הנפילה נכתבת מחדש (היום היא
   עברית שבורה: "בין פתחו את המשחק להתחילו קריירה, ירדו 1 אנשים"). פתוח גם: האם הוא רוצה כפתור סינון
   של המכשיר שלו, שדורש גם שינוי בשרת.
4. **המעבר ל-pages.dev.** ראה האזהרה למעלה.
5. **שורת הפרסומות.** `src/ui/screens/Packs.tsx:51` אומר "פרסומות יעלו בקרוב, היהלומים שלהן אצלך
   בינתיים" ליד צ'יפ +3. הצעתי לקצר ל"היהלומים של הפרסומות כבר אצלך". לא הוכרע.
   `ADS_LIVE = false` ב-`src/data/ads.ts`. להחזיר: מילה אחת, `true`.
6. **קול אוהדים במשחק.** איציק ביקש ואז אמר שלא נעשה. אם יחזור לזה: למשחק **אין היום שום שמע**, זו
   מערכת חדשה שלמה, ויש שלושה דברים על השולחן: חסימת אוטופליי בטלפון (ובאייפון כלל נוסף שמפיל את זה
   בשקט), רישוי הצליל, והעובדה שלולאה שטוחה לא תעשה את הרושם אלא יציע שמגיב לאירועים.

---

## דברים שצפו ולא נגעתי בהם

- **13 מכשירים דיווחו התקדמות בלי שדיווחו פתיחה**, ולכן לא נספרים ב"אנשים" (213 בטבלת steps מול 200
  ב-sessions). ההסבר הסביר הוא טלפונים שמריצים עדיין גרסה מלפני התיקון של ה-23, במיוחד מי שהתקין
  למסך הבית ושומר גרסה במטמון. המשמעות: מספר האנשים נמוך מהאמת.
- **"אלייך" ו"עלייך"** בטקסטים של איציק הן צורת נקבה (בזכר: אליך, עליך). הוא כתב אותן פעמיים ולא
  הגיב כששאלתי, אז הן ביישום כפי שכתב.
- הצ'יפ "איש קריפטו" בתכונות עדיין אומר קריפטו, אבל השורה שאיציק כתב אומרת "הפסיד על מנייה בטוחה".
- שורה של עדי ייני (קבוע של ראש העין) כתוב בה "אבל שהוא מקבל את הכדור", כנראה חסרה כ'.
- שורה 11 של היציע ("פעם היו באים 3000 איש למשחק כזה") היא היחידה שמדברת על ריקנות בלי לבדוק את מדד
  היציע.
- `state.ts` בערך שורה 3941: כותרת `" עזב את הנוער"` מתחילה ברווח ובלי שם, כנראה חסר שם השחקן.
- האייקון בכל פופאפ מסוג story הוא מיקרופון קבוע (`Notice.tsx`), גם בהודעת העברה.
- רוויית מדד היציע: חציון 63, ו-7.6% מהמחזורים תקועים על 100 בדיוק.
- שמירה באמצע מסך החברים: אם סוגרים את האפליקציה באמצע, הקריירה נשמרת אבל הטיוטה לא.
- רענון בזמן שאתה במסך הסגל מחזיר אותך למסך הסגל של הביקור הראשון.
- שלוש שיחות טלפון שלא נראו במשחק אמיתי: `mate_injured` (תלויה בפציעה שמוציאה שחקן, ואירוע פציעה
  מהמנוע לא עושה כלום היום) ו-`mate_promoted`.

---

## מוסכמות טכניות (נלמדו בדם)

- **עריכות:** כותבים סקריפט `.js` עם ה-Write tool ומריצים ב-node. פונקציית `edit(file, [[a,b],...])`
  שכותבת רק אם כל עוגן מופיע **בדיוק פעם אחת**. חלק מהקבצים ב-CRLF וחלק ב-LF, אז עוגן רב שורתי עם
  `\n` מחטיא בשקט בקובץ CRLF. העוזר שלי מנרמל ומחזיר את הסיומות, שווה לכתוב אותו מחדש בהתחלה.
- backticks, `${}` ו-`\n` בתוך `node -e` או heredoc דרך bash נשברים. כל מה שמכיל אותם, דרך Write tool.
  **קרה לי בסשן הזה פעמיים**, פעם אחת בגרעין של מסך הפתיחה ופעם בשומר של lineup-check.
- `s.replace(a, b)` עם `b` שמכיל `$'` הוא מלכודת. תמיד `s.replace(a, () => b)`.
- הטרמינל של איציק הוא **Windows PowerShell**: `&&` לא קיים שם. כל פקודה שאתה נותן לו חייבת להיות
  פקודה אחת בקופסה. (אצלך, בכלי Bash, `&&` עובד.)
- **דפדפן מובנה:** `preview_start` עם `name: "be-a-pro"` (port 5180). מגבלות שגיליתי בדם:
  `requestAnimationFrame` לא רץ שם בכלל, ו-`ResizeObserver` לא יורה שם בכלל. אל תסיק מזה שקוד שבור.
  `resize_window` 375×812, 360×740 ו-360×640 לבדיקת צפיפות.
- **צילומי מסך לקבצים:** כרום headless ב-
  `C:/Program Files/Google/Chrome/Application/chrome.exe`, עם
  `--headless=new --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --window-size=W,H --virtual-time-budget=20000 --screenshot=out.png URL`.
  **מלכודת ווינדוס:** רוחב חלון מתחת ל-500 נחתך, אז מרנדרים רחב וחותכים ב-ffmpeg.
  `--force-prefers-reduced-motion` לבדיקת תנועה מופחתת, `--host-resolver-rules="MAP host ip"` לעקיפת DNS.
- **זריעת save:** סקריפט זמני `scripts/_shotsave.mts` שבונה state דרך
  `newGame(seed)/setProfile/pickCity/setArchetype/afterSigning/addFriends/enterSeason({...gs, crisisDone:true})/closeKitReveal/takeSponsor('base')/dismissNotice/markTutorialSeen`,
  מסיים ב-`{...gs, phase:'hub'}` (או כל phase אחר), וכותב `saveCareer` עם localStorage מזויף ל-
  `public/_shotsave.json`. בדפדפן:
  `localStorage.setItem('beapro.career.v1', await (await fetch('/_shotsave.json?x='+Date.now())).text()); location.reload();`
  ואז "המשך קריירה". **למחוק את שני הקבצים לפני commit.** `afterSigning(gs, {})` דורש ארגומנט שני.
- סבוטאז'/מדידה על גרסה קודמת: `git worktree add <scratchpad>/wt-old <commit>`.
- אין ImageMagick, יש ffmpeg. **python לא עובד** (הוא קיצור דרך של חנות ווינדוס), אז סקריפטים של
  סקילים שדורשים python לא ירוצו.
- `PIC/` ב-gitignore. שם יושבות שש תמונות השיווק שהכנו ל-Facebook ב-`PIC/launch/`.
- יש חיבור **Cloudflare MCP** בסשן (תשאול D1, רשימת Workers). הוא לא יודע להעלות Worker, לזה צריך
  `wrangler` מהמחשב (`cd worker` ואז `npx wrangler deploy`).
- ה-CRM: Worker + D1 בתיקייה `worker/`, כתובת `https://beapro-count.beapromanager.workers.dev`,
  מסד `beapro-count`, החשבון beapromanager@gmail.com. מדריך בעברית: `CRM.md`.
- **קו אדום בפרטיות:** `src/game/telemetry.ts` לא יכול לייבא את `state.ts`/`save.ts`, ולכן אין נתיב
  משמות שהשחקן הקליד אל הרשת. `check:telemetry` קורא את רשימת הייבוא ונופל אם מישהו מוסיף אחד.
  הודעת שגיאה לא נשלחת לעולם, רק סוג השגיאה מרשימה סגורה.
