# המשך עבודה על BE A PRO, הודעת פתיחה לצ'אט חדש

אני איציק. אנחנו ממשיכים עבודה על המשחק שלי BE A PRO, משחק מנג'ר כדורגל ישראלי
בעברית (RTL), Vite + React + TypeScript, ב-`C:\Users\itzik\Desktop\CLAUDE\be-a-pro`.
תשתמש בסקיל `be-a-pro-skill`. הצ'אט הקודם נגמר. ההודעה הזו מתארת בדיוק איפה
עצרנו. **תקרא הכל לפני שאתה נוגע במשהו.**

## מצב הגיט: הכל נקי ודחוף

* הריפו: github.com/beapromanager/beapromanager.github.io, ענף `main`, ה-remote
  נקרא `game` (`git push game main`). `origin` הישן הוא ארכיון, לא דוחפים אליו.
* האתר החי: https://beapromanager.github.io/ (base `/`). GitHub Actions בונה
  מ-`main`, עדכון תוך 2-3 דקות. לעקוב: `gh run list --repo beapromanager/beapromanager.github.io --limit 1`.
* `gh` מחובר בתור `itzik200592-byte`. הגדרות Pages עושה איציק בדפדפן.
* **אין קומיטים מקומיים שלא נדחפו.** `main` ו-`game/main` שניהם על `dcd60f6`.
* **הצעד הראשון שלך:** `git status` (חייב להראות רק `M .claude/launch.json`, סשן
  אחר הוסיף שם config של green-bio, **לא נוגעים ולא מכניסים לקומיט**),
  `git log --oneline game/main..main` (חייב להיות ריק), ואז להריץ את הסוויטה
  (למטה). אם `git status` מראה משהו אחר, עצור ושאל. אחרי שהכל ירוק תגיד "מוכן"
  ותשאל מה הבא.

## הדפוס שלנו (חשוב)

* אני נותן משימה. לפני שאתה מתקן, תחקור לעומק, תגיד מה מצאת ומה הסדר המומלץ,
  ותחכה שאאשר. תיקון אחד בכל פעם, קומיט לכל תיקון.
* **תמדוד, אל תניח.** אם אני טוען "X קורה", תריץ סימולציה ותגיד את המספר. ואם
  המדידה שלך מפתיעה, **תבדוק שהיא לא רועשת מדי לפני שאתה מדווח אותה.**
* **אל תנחש למה משהו שבור. תשחזר אותו קודם.** בסשן הזה כל באג אמיתי נמצא ככה,
  וכל פעם שניחשתי טעיתי.
* כשגומרים תיקון: הסוויטה הרלוונטית ירוקה, `npx tsc --noEmit`, `npm run build`
  (ואז `git checkout -q tsconfig.tsbuildinfo`), **אימות בדפדפן למה שנראה במסך**,
  commit מקומי בהודעה בסגנון שלנו (פרוזה באנגלית שמסבירה **למה**, לא רשימת
  שינויים, מסתיימת ב-`Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>`),
  ואז אתה שואל "GIT PUSH?". דוחפים רק אחרי שאני כותב GIT PUSH (גם "FIT PUSH"
  זה GIT PUSH). `git add` עם שמות קבצים מפורשים, לא `-A`.
* **טקסטים במשחק: כתיבה ביחד.** אתה מציע, אני מתקן, אתה מיישם **בדיוק** במילים
  שלי. טקסט שהכנסת בלי אישור מראש: טבלת "היה / עכשיו" בסיכום. **אין מקפים
  ארוכים בשום טקסט שהשחקן רואה.**
* איזון וכלכלה: אתה מציג מספרים ואפשרויות, אני מחליט.
* בסוף כל תיקון: מה תוקן, מה הבדיקה מודדת, איך אימתת שהיא נכשלת על קוד שבור,
  מה ראית בדפדפן, ודברים שצפו ולא נגעת בהם.

## הכלל על בדיקות (חשוב מאוד)

כל בדיקה חדשה חייבת להיות מאומתת שהיא נכשלת על קוד שבור: משבשים בכוונה, רואים
FAIL **עם ההודעה הנכונה**, משחזרים, ומאמתים `cmp` מול עותק טוב. הדרך שעובדת:
לפני הסבוטאז' `cp` של הקובץ לעותק `.good` בסקראצ'פאד; סבוטאז' עם סקריפט
שמחליף מחרוזת ומוודא שהעוגן מופיע בדיוק פעם אחת; שחזור עם `cp` מהעותק הטוב
(לא `git checkout <file>`); `cmp` בסוף. בדיקה שעוברת בפעם הראשונה חשודה.

**שומרים שנכשלו על עצמם, קרה שלוש פעמים בסשן הזה, תיזהר:** שומר שמחפש מחרוזת
בקוד תופס לפעמים את **ההערה של עצמו** שמסבירה למה הכלל קיים. אם שומר נופל על
הפרוזה של עצמו, הוא מלמד להתעלם משומרים. תשתמש בהתאמת מחרוזת פשוטה
(`includes`) ולא ברגקס כשאפשר, כי **ההימלטויות נבלעות** כשכותבים רגקס לתוך
קובץ דרך תבנית מחרוזת.

**הסוויטה, 48 בדיקות** (`telemetry` נוספה בסשן הזה):

```
for s in pitch formations sync goals numbers debt economy climb sacking guaranteed league youth kits palette names market lineup press invite gems coach install scroll summer legends seasonkit penalty discipline back voice shape oldsave repeat suspension keeper part dilemma sub tutorial pack feed seed ads sponsor report fans friends telemetry; do npm run -s check:$s >/dev/null 2>&1 && echo "OK   $s" || echo "FAIL $s"; done
```

לוקחת 20-35 דקות (`discipline` לבד 7-17 דק'). תריץ ב-`run_in_background` עם
הפלט לקובץ ב-`C:/Users/itzik/AppData/Local/Temp/` ושורת `DONE` בסוף. **היא
נופלת ל-background אחרי 10 דק', פשוט תריץ את הלולאה שוב.** בזמן שהסוויטה רצה
**אל תערוך קבצי מקור** (הבדיקות מייבאות אותם בזמן ריצה). בדיקה בודדת:
`node --experimental-strip-types scripts/<name>-check.mts`.

**save ישן:** כל שדה חדש על ה-state חייב ברירת מחדל ב-src/game/save.ts
(`loadCareer`) וגם ב-`ADDED_SINCE` ב-scripts/oldsave-check.mts.

---

## מה קרה בסשן הזה (14 קומיטים, `25941ba` → `dcd60f6`)

הסשן הזה היה **הכנה להשקה**. איציק מפיץ את המשחק עכשיו.

### א. הפרסומות כבויות להשקה (`e728b92`)

* `ADS_LIVE = false` ב-`src/data/ads.ts`. הכפתור אומר **"פרסומות יעלו בקרוב"**
  ומושבת. **להחזיר בעוד שבועיים: מילה אחת, `true`.** כל המנגנון חי מתחתיו.
* השחקן מקבל את 3 היהלומים של העונה **בחינם** (`adFreeGems()` ב-packs.ts),
  בתחילת קריירה ובכל עונה חדשה, כדי שהכלכלה תישאר זו שנמדדה.
* `adsLeft` נשאר חשבון טהור; נוספה `adsOffered` שהיא מה שהמסכים שואלים. זה מה
  שאיפשר לכל בדיקות הפרסומות להמשיך למדוד את המכונה בזמן שהיא כבויה.

### ב. ה-CRM, הדבר הגדול של הסשן (`ea9dd5f`, `fa38a03`, `81e1976`, `922936a`, `f4ca4bf`, `afb2c6b`, `bc91233`)

**חי ועובד.** מודד כמה אנשים, כמה כניסות, כמה חזרו, ואיפה כל אחד עצר.

* **השרת:** Cloudflare Worker + D1, בתיקייה `worker/`.
  כתובת: `https://beapro-count.beapromanager.workers.dev`
  מסד: `beapro-count`, מזהה `695c34b0-42ad-4fda-b7c4-ab5b39a98922`, מערב אירופה.
  טבלאות: `steps`, `sessions`, `crashes`.
  החשבון: `beapromanager@gmail.com`. wrangler מחובר על המחשב של איציק.
  **פריסה מחדש:** `cd worker` ואז `npx wrangler deploy`.
* **הדשבורד:** `https://beapromanager.github.io/?admin=beapro-rahat-hetsi-maamad-derbi-8988`
  הסיסמה היא secret בשרת (`ADMIN_KEY`), **לא בקוד**.
* **הכתובת במשחק:** `src/data/telemetry.ts`, קבוע `TELEMETRY_URL`. ריק = המשחק
  לא סופר כלום ומתנהג בדיוק כמו קודם.
* **מדריך בעברית לאיציק:** `CRM.md`.
* **פרטיות, קו אדום:** `src/game/telemetry.ts` **לא יכול לייבא** את
  `state.ts`/`save.ts`, ולכן אין נתיב משמות שהשחקן הקליד אל הרשת.
  `check:telemetry` קורא את רשימת הייבוא ונופל אם מישהו מוסיף אחד.
  **הודעת שגיאה לא נשלחת לעולם**, רק **סוג** השגיאה מרשימה סגורה של שישה.
* **15 שלבים בפאנל**, בסדר שהמשחק באמת הולך בו:
  `open, career_new, manager, club, archetype, signing, friends, squad, market,
  season, round_1, round_3, round_7, season_end, season_2`.
  **שים לב: המסכים לא נקראים בסדר שהמשחק הולך בו.** קודם שם, אז מועדון, אז
  ארכיטיפ, אז חתימה. אל תסיק סדר משמות הפונקציות, תמדוד.
* **`week` מתחיל ב-1**, לא ב-0. מחזורים ששוחקו הם `week - 1`. זה היה באג.
* **המספרים מרפאים את עצמם:** דיווח על שלב מדווח את כל הדרך אליו
  (`stepsUpTo`), ושום דבר ממה שנשלח לא נשמר בין ביקורים. השרת הוא זה שלא סופר
  פעמיים (מפתח ראשי `(aid, step)`). **אל תחזיר זיכרון "כבר שלחתי" ללקוח**, זה
  היה באג שהשתיק טלפון שלם לצמיתות.
* **קריסות נספרות** עם השלב וסוג השגיאה. רשת הקריסות תופסת **רק שגיאות
  מהקוד שלנו** (`isOurCrash` ב-report.ts), כי אחרת תוסף דפדפן מקפיץ לשחקן
  "משהו נשבר" וגם מלכלך את הנתונים.
* **הדשבורד סגור:** קוד שגוי נראה **בדיוק כמו המשחק**, בלי שום רמז שיש אזור
  מנהל. הביקורים של איציק בדשבורד לא נספרים כמשחק.

### ג. תצוגה מקדימה לקישור (`afb2c6b`)

תגיות `og:` ב-`index.html` + `public/share.jpg` (1200×630, 89KB, חתוך
מ-`intro/scene4.webp`). המשחק מופץ בוואטסאפ, וקישור בלי כרטיס זה כתובת עירומה.
הכתובות **מוחלטות** כי מי שמושך את התצוגה המקדימה בא מבחוץ.

### ד. מסך הסגל, שלושה סבבים (`25941ba` בסשן קודם, ואז `6e955ce`, `f62884f`, `dcd60f6`)

**קרא את זה לפני שאתה נוגע במסך הסגל.** ניסיתי למדוד את גובה המגרש לתוך המקום
שנשאר מעל הספסל. אותה החלטה ילדה **שלושה באגים** בזה אחר זה: הספסל כיסה את
ההגנה, הכפתור של הביקור הראשון נחבא מאחורי הספסל, ולבסוף **ריצוד**, כי מדידה
שמזינה את הפריסה שהיא מודדת תמיד יכולה לריב איתה.

**המצב עכשיו, וזה נכון: אין מדידה בכלל.** בתצוגת המגרש מסך הסגל הוא **עמודה
בדיוק בגובה החלון**:

* `.frame:has(.squad-fit){height:100dvh; overflow:hidden}` מחזיק את הגובה
* `.squad-scroll` גולל את כל מה שמעל לוח ההרכב
* `.bench-bar` הוא **השורה התחתונה בזרימה**, לא `position:fixed` ולא ב-Portal
* `.squad-pitch-box{flex:1 1 auto; min-height:240px}` לוקח את מה שנשאר

**נמדד בדפדפן:** 393×852 נותן מגרש 330 עם כל האחד עשר גלויים ואפס גלילה;
360×740 יושב על הרצפה 240 ולא מסתיר אף אחד.

**אל תחזיר מדידה, observer או טיימרים לשם.** אם צריך עוד מקום למגרש, קח אותו
ממה שמעליו (המדדים 101, הכותרת 57, בורר המערך).

### ה. ההגנות על החברים (`180116c`)

הרשימה אמרה שלושה דברים, והמדידה מצאה **רביעי מתחתם שהחזיק את כולם**: שום דבר
לא סימן שחבר עזב. חבר שנמכר נשאר חבר, היה **בא איתך אחרי פיטורים**, ו-
`mate_alone` (השיחה שבה נותנים את המילה "אותך אני לא מוכר") **לא יכלה לירות
לעולם**, כי היא בודקת `friends.some(x => x.sold)`. כל מנגנון ההבטחה היה קוד מת.

* `markFriendGone` ב-friends.ts, נקרא מ-`partWays` ומ-`settleSummerExits`.
* `sellPlayer` **מסרב** למכור חבר (במנוע, לא רק במסך). השוק לא מציג אותם.
* האישור בכרטיס (שתי לחיצות, קיים לכולם) **אומר מה זה** כשמדובר בחבר.
* הפרת המילה: `BROKEN_WORD_MORALE = -16` (כפול מהפרת הבטחת הרכב) + שורה
  ב-`followUps` שחוזרת שבוע אחרי. נמדד: מורל 61 ← 45.

---

## דבר אחד שממתין לאיציק, תזכיר לו

**הטקסטים החדשים של החברים לא אושרו.** הם נכנסו בלי אישור מראש והוא ביקש
לעבור עליהם:

| איפה | הטקסט |
|---|---|
| בשוק | מי שבא איתך לא נמכר מכאן. אם באמת צריך להיפרד, זה מהכרטיס שלו. |
| אישור, חבר רגיל | {שם} בא איתך מהשכונה. |
| | הוא לא ימצא מועדון אחר שלקח אותו בגלל מי שהוא. |
| אישור, אחרי שנתת מילה | נתת לו מילה שאותו לא תמכור לעולם. |
| | אם הוא הולך, כל הסגל יידע שהמילה שלך לא שווה כלום. |
| שבוע אחרי | **מכרת את מי שהבטחת לו** · אמרת ל{שם} שאותו לא תמכור לעולם. מכרת. בשכונה יודעים. |

וגם: **"פרסומות יעלו בקרוב"** ו**"פרסומות יעלו בקרוב, היהלומים שלהן אצלך
בינתיים"**, ו**"סופרים שימוש אנונימי כדי לדעת מה לשפר. השמות שאתה מקליד
נשארים במכשיר שלך."** (במסך הפתיחה), ו**"חדר ההלבשה"** / **"למשחק"** בלוח הגיר.

---

## מה פתוח, בסדר שהמלצתי

1. **הפופאפ של החבר שעוזב.** כשחבר עוזב אחרי "תחפש קבוצה", איציק ביקש **הודעת
   פופאפ שהוא עובר לקבוצה היריבה בעיר**. עוד לא מומש. עכשיו שיש
   `markFriendGone` וה-`sold` נרשם, זה אפשרי סוף סוף.
2. **הרוויה של מדד היציע.** נמדד: חציון 63, ו-7.6% מהמחזורים תקועים על 100
   בדיוק, כי ניצחון נותן +2 והשכחה לוקחת רק 1. אפשר שכחה חזקה יותר בקצוות או
   תשואה פוחתת מעל 80. **המספרים לאישור של איציק.**
3. **קריירה שלמה בטלפון אמיתי.** נבנה המון והוא לא ראה את זה רץ ברצף. במיוחד:
   מסך הסגל החדש באצבע, לוח הגיר, הכפתור של הכבשה השחורה (שייפתח באפליקציית
   וואטסאפ), ודיווח תקלה (אינסטגרם).
4. **לבדוק את הדשבורד אחרי יום של שחקנים אמיתיים** ולהחליט מה לתקן לפי איפה
   הנפילה הכי גדולה. **זו כל הסיבה שבנינו אותו.**
5. שלוש שיחות טלפון שלא נראו במשחק אמיתי: `mate_injured` (תלויה בפציעה שמוציאה
   שחקן, ואירוע פציעה מהמנוע **לא עושה כלום** היום) ו-`mate_promoted`.
6. פרסומות נוספות, preload של הפרסומת הבאה, earned kits, Playwright לפריסה,
   לצמצם את `check:discipline`.

## דברים שצפו ולא נגעתי בהם

* **הצ'יפ "איש קריפטו"** בתכונות עדיין אומר קריפטו, אבל השורה שאיציק כתב אומרת
  "הפסיד על מנייה בטוחה". לא תואמים.
* **שורה של עדי ייני** (קבוע של ראש העין) כתוב בה "אבל שהוא מקבל את הכדור",
  כנראה חסרה כ' ("כשהוא"). הושאר בדיוק כפי שנכתב.
* **שורה 11 של היציע** ("פעם היו באים 3000 איש למשחק כזה") היא היחידה שמדברת על
  ריקנות בלי לבדוק את מדד היציע.
* **שמירה באמצע מסך החברים:** אם סוגרים את האפליקציה באמצע, הקריירה נשמרת אבל
  הטיוטה לא.
* **רענון בזמן שאתה במסך הסגל** מחזיר אותך למסך הסגל של הביקור הראשון (עם
  המאמן ו"שלושה שכדאי להכיר"), כי ה-save רושם `phase: 'squad'` ו-`squadFromHub`
  הוא state של React שנמחק. לא מזיק, אבל מוזר.
* **מסך הסגל של הביקור הראשון עדיין ארוך וגולל.** זה עמוד אונבורדינג שקוראים
  מלמעלה למטה, אז השארתי.

## מוסכמות טכניות (נלמדו בדם)

* **עריכות:** כותבים סקריפט `.js` **עם ה-Write tool** ומריצים ב-node. פונקציית
  `edit(file, [[a,b],...])` שכותבת רק אם כל עוגן מופיע בדיוק פעם אחת.
  **חלק מהקבצים ב-CRLF וחלק ב-LF**, אז עוגן רב-שורתי עם `\n` מחטיא בשקט בקובץ
  CRLF. העוזר שלי מנרמל ומחזיר את הסיומות; שווה לכתוב אותו מחדש בהתחלה.
* **backticks, `${}` ו-`\n` בתוך `node -e` או heredoc דרך bash נשברים.** כל מה
  שמכיל אותם, דרך Write tool. **גרש בעברית** (`הצ'אט`) שובר מחרוזת בגרשיים
  בודדים ב-bash.
* **`s.replace(a, b)` עם b שמכיל `$'` הוא מלכודת.** תמיד `s.replace(a, () => b)`.
* **הטרמינל של איציק הוא Windows PowerShell:** `&&` **לא קיים שם**. כל פקודה
  שאתה נותן לו חייבת להיות פקודה אחת בקופסה. (אצלך, בכלי Bash, `&&` עובד.)
* **דפדפן מובנה:** `preview_start` עם `name: "be-a-pro"` (port 5180).
  **מגבלות שגיליתי בדם: `requestAnimationFrame` לא רץ שם בכלל, ו-`ResizeObserver`
  לא יורה שם בכלל.** אל תסיק מזה שקוד שבור, ואל תאמת דרכם.
  צילומי מסך עובדים אבל לפעמים נכשלים בפעם הראשונה, פשוט תנסה שוב.
  `resize_window` 393×852 ו-360×740 לבדיקת צפיפות.
* **זריעת save:** סקריפט זמני `scripts/_hubsave.mts` שבונה state דרך
  `newGame(seed)/setProfile/pickCity/setArchetype/afterSigning/addFriends/
  enterSeason({...gs, crisisDone:true})/closeKitReveal/takeSponsor('base')/
  dismissNotice/markTutorialSeen`, מסיים ב-`{...gs, phase:'hub'}`, וכותב
  `saveCareer` עם localStorage מזויף ל-`public/hub.json`. בדפדפן:
  `localStorage.setItem('beapro.career.v1', await (await fetch('/hub.json?x='+Date.now())).text()); location.reload();`
  ואז "המשך קריירה". **למחוק `scripts/_hubsave.mts` ו-`public/hub.json` לפני
  commit.** שים לב: `afterSigning(gs, {})` דורש ארגומנט שני.
* **סבוטאז'/מדידה על גרסה קודמת:** `git worktree add <scratchpad>/wt-old <commit>`.
* אין ImageMagick, יש `ffmpeg`. Chrome ב-`C:/Program Files/Google/Chrome/Application/chrome.exe`.
  python לא עובד. `PIC/` ב-gitignore.
* **יש חיבור Cloudflare MCP** בסשן (יצירה ותשאול של D1). הוא **לא** יודע
  להעלות Worker, לזה צריך wrangler מהמחשב.
