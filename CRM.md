# להפעיל את הנתונים, צעד אחר צעד

המדריך הזה מפעיל את הספירה: כמה אנשים פתחו את המשחק, כמה חזרו, ואיפה כל אחד
הפסיק לשחק. הכל בחינם, וזה לוקח בערך חצי שעה.

**חשוב לדעת לפני שמתחילים:** המשחק כרגע **לא סופר כלום**. הוא מוכן להפצה גם
בלי המדריך הזה, ואם תדלג עליו שום דבר לא יישבר. הספירה מתחילה רק כשמסיימים את
שלב 7.

## מה נאסף, ומה לא

| נאסף | לא נאסף לעולם |
|---|---|
| מספר אקראי לכל מכשיר | השם שלך או של החברים |
| איזה שלב במשחק הושג | שם המועדון, הסגל, כל מה שמישהו הקליד |
| מתי | כתובת IP, מיקום, סוג מכשיר |

המספר האקראי לא אומר כלום מחוץ למכשיר. הוא קיים רק כדי שאפשר יהיה לענות על
"כמה אנשים" ולא רק "כמה פעמים".

---

## שלב 1: לפתוח חשבון Cloudflare

לך ל-https://dash.cloudflare.com/sign-up, הירשם עם המייל שלך. **אין צורך
בכרטיס אשראי.** אשר את המייל שיגיע.

אם כבר יש לך חשבון, דלג לשלב 2.

---

## שלב 2: להתחבר מהמחשב

פתח טרמינל בתיקיית המשחק והרץ:

```bash
cd worker && npm install
```

זה מוריד את הכלים. לוקח דקה.

עכשיו ההתחברות. הפקודה הזאת תפתח לך דפדפן, שם תלחץ "Allow":

```bash
npx wrangler login
```

**מה אתה אמור לראות:** בטרמינל יופיע `Successfully logged in`.

---

## שלב 3: ליצור את מסד הנתונים

```bash
npx wrangler d1 create beapro-count
```

**מה אתה אמור לראות:** כמה שורות, וביניהן משהו בסגנון:

```
database_id = "a1b2c3d4-5678-90ab-cdef-1234567890ab"
```

**העתק את המספר הארוך הזה.** צריך אותו בשלב הבא.

---

## שלב 4: להדביק את המספר

פתח את הקובץ `worker/wrangler.toml`, ובשורה האחרונה החלף את
`PASTE_THE_ID_WRANGLER_PRINTS` במספר שהעתקת. זה אמור להיראות כך:

```
database_id = "a1b2c3d4-5678-90ab-cdef-1234567890ab"
```

שמור את הקובץ.

---

## שלב 5: ליצור את הטבלאות

```bash
npx wrangler d1 execute beapro-count --remote --file=schema.sql
```

**מה אתה אמור לראות:** שורה שאומרת שבוצעו 6 פקודות, או טבלה עם שש שורות.

אם זה שואל אותך אם אתה בטוח, תענה כן.

---

## שלב 6: לבחור סיסמה לדשבורד

```bash
npx wrangler secret put ADMIN_KEY
```

הוא יבקש ממך להקליד סיסמה. **תבחר משהו ארוך, לפחות 20 תווים**, כי כל מי שינחש
אותה יראה את הנתונים. משהו כמו `beapro-2026-itzik-sod-shel-i` עובד מצוין.

**תשמור את הסיסמה הזאת אצלך.** לא תוכל לראות אותה שוב, רק להחליף אותה.

---

## שלב 7: להעלות את השרת

```bash
npx wrangler deploy
```

**מה אתה אמור לראות:** בסוף תופיע כתובת, משהו בסגנון:

```
https://beapro-count.השם-שלך.workers.dev
```

**העתק את הכתובת הזאת ותשלח לי אותה.** אני אכניס אותה למשחק, נדחוף, ותוך
שתי דקות הספירה מתחילה.

אם אתה רוצה לעשות את זה בעצמך: פתח את `src/data/telemetry.ts`, ובשורה האחרונה
שים את הכתובת בין הגרשיים:

```
export const TELEMETRY_URL: string = 'https://beapro-count.השם-שלך.workers.dev';
```

ואז מהתיקייה הראשית:

```bash
git add src/data/telemetry.ts && git commit -m "Point the counting at the worker" && git push game main
```

---

## לראות את הנתונים

```
https://beapromanager.github.io/?admin=הסיסמה-שבחרת
```

נפתח מהטלפון או מהמחשב, אין מה להתקין. מה שתראה:

* **אנשים** כמה מכשירים שונים פתחו את המשחק
* **כניסות** כמה פעמים בסך הכל
* **חזרו שוב** כמה מהם חזרו ביום אחר, וזה המספר הכי חשוב לטווח ארוך
* **איפה הכי הרבה עוזבים** המעבר שבו הכי הרבה אנשים נעלמו, וזה מה שכדאי לתקן
* **איפה כל אחד עצר** כל 15 השלבים עם כמה הגיעו לכל אחד
* **לפי יום** כמה נכנסו כל יום

---

## אם משהו לא עובד

**`wrangler: command not found`** לא רצת `npm install` בתיקיית `worker`. חזור
לשלב 2.

**`Authentication error`** ההתחברות פגה. הרץ שוב `npx wrangler login`.

**`no such table`** דילגת על שלב 5, או שהרצת אותו בלי `--remote`.

**הדשבורד אומר "סיסמה שגויה"** הקלדת סיסמה אחרת מזו שנתת בשלב 6. אפשר לשים
חדשה עם `npx wrangler secret put ADMIN_KEY` ולהריץ שוב `npx wrangler deploy`.

**הדשבורד אומר "השרת לא עונה"** או שהכתובת בקובץ `src/data/telemetry.ts` לא
נכונה, או שהשרת לא הועלה. תריץ `npx wrangler deploy` שוב.

**רוצה לראות מה מגיע לשרת בזמן אמת:**

```bash
cd worker && npx wrangler tail
```

---

## כמה זה עולה

כלום. המסגרת החינמית של Cloudflare היא 100,000 פניות ליום ו-5 ג'יגה מסד
נתונים. קריירה שלמה של שחקן אחד היא בערך 15 שורות, אז גם אלף שחקנים ביום הם
אחוז מהמסגרת.
